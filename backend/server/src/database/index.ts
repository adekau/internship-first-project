// function that will set associations between models and sync the database

// FOR THE MOMENT THIS FILE IS USELESS.
// Database associations are handled in the individual model files ('/models/*.ts').

import { db } from './config';
import { models } from '../models';

// Constants
const { Incident, User } = models;

export default () => {}